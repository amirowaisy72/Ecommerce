<select
            className="form-select"
            value={`${selectedCategoryData.parent}|${selectedCategoryData.child}`}
            onChange={handleCategoryChange}
          >
            <option value="Select a Category" disabled>
              Select a Category
            </option>
            {categories.map((category) => (
              <optgroup label={category.parent} key={category.parent}>
                {category.children.map((child) => (
                  <option key={child} value={`${category.parent}|${child}`}>
                    {child}
                  </option>
                ))}
              </optgroup>
            ))}
          </select>


there is issue with this block of code.
options are being displayed from optgroup directly
and as usual it is displaying first option initially which "Joggers"
it is not starting from 
<option value="Select a Category" disabled>
              Select a Category
            </option>
this option. that why we are facing problem
fix here 
but you again and again for for useEffects and bla bla bla