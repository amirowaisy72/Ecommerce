{submitResponse && (
                <div className="text-danger">{submitResponse}</div>
              )}

display errors in beautiful way
i mean in list view or any professional viewall errors are displayed very plain
but if threre is no error then no need to display response in list view, you can display plain